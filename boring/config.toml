# The URL the site will be built for
base_url = "https://scp.net.cn"

# The site title and description; used in feeds by default.
title = "码农的自留地"
description = "语法 语义 语用"

# When set to "true", the generated HTML files are minified.
minify_html = true

# When set to "true", a sitemap is automatically generated.
generate_sitemap = true

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting Theme can be customised by setting the
# `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# The theme to use for code highlighting.
# highlight_theme = "nord"
highlight_theme = "monokai"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true

[link_checker]
internal_level = "warn"

[extra]
homepage_title = 码农的自留地"
homepage_subtitle = "Less is more!"
# homepage_img_url = "/homepage-img.jpg"
homepage_img_url = "/index-image.png"

# Refer https://support.google.com/analytics/answer/9539598?hl=en
google_analytics_measurement_id = ""

# Refer https://icons8.com/line-awesome for list of icons
# Both `url` and `icon` should be set for each entry
[[extra.social]]
url = "https://github.com/dllen"
icon = "lab la-github"

[[extra.social]]
url = "https://github.com/dllen"
icon = "lar la-user-circle"
